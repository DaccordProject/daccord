// The legacy Bonfire counter-app smoke test that ships with `flutter create`
// is not meaningful for this app and was commented out long ago. Keep a stub
// `main()` so `flutter test` doesn't error out trying to load a file with no
// entry point.
void main() {}
